CREATE TABLE [dbo].[AutoMake] (
    [ID]      INT            NOT NULL,
    [Name]    NVARCHAR (128) NOT NULL,
    [Name_ru] NVARCHAR (128) NOT NULL,
    CONSTRAINT [PK_AutoMark] PRIMARY KEY CLUSTERED ([ID] ASC)
);

