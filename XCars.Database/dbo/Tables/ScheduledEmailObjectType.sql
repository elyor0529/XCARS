CREATE TABLE [dbo].[ScheduledEmailObjectType] (
    [ID]   INT            NOT NULL,
    [Name] NVARCHAR (128) NOT NULL,
    CONSTRAINT [PK_ScheduledEmailObjectType] PRIMARY KEY CLUSTERED ([ID] ASC)
);

